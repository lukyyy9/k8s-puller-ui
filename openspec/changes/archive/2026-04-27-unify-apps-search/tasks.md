## 1. Application List Filters

- [x] 1.1 Add `dependencyName` and `dependencyVersion` state variables to the `Home` component in `src/App.jsx`.
- [x] 1.2 Add the search form (input for "Dependency Name", optional input for "Dependency Version", and "Search" button) to the `Home` component UI, replacing the existing "Search Dependencies" link.
- [x] 1.3 Implement the search submit handler in `Home` component to filter the mock data or re-trigger mock data loading.

## 2. Clean Up Old Dependency Search

- [x] 2.1 Remove the `<Route path="/search-dependency" />` from the routing configuration in `src/App.jsx`.
- [x] 2.2 Delete the unused `src/pages/DependencySearch.jsx` file.
