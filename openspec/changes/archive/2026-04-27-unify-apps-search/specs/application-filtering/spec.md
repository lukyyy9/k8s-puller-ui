## ADDED Requirements

### Requirement: Application list filters
The system SHALL provide inputs to filter applications by "Dependency Name" and optionally "Dependency Version" on the Applications page.

#### Scenario: Display filter inputs
- **WHEN** the user views the Applications page
- **THEN** the system displays the "Dependency Name" and "Dependency Version" input fields alongside a "Search" submit button.
