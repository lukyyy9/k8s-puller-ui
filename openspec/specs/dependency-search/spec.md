## REMOVED Requirements

### Requirement: Search form layout
**Reason**: Replaced by service list filters on the main Services page.
**Migration**: Use the filter inputs directly on the Services page.

### Requirement: Search mock result payload
**Reason**: The results are now integrated into the main Services page list, so a separate page with mocked summary is no longer needed. The existing Services page mock data will be used.
**Migration**: Submit search on the Services page to filter the existing table.